fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# --- Media downloading (yt-dlp) ---
# Dipanggil sebagai subprocess dari server/downloader.py.
yt-dlp>=2026.8.19

# --- Stem separation (Demucs) ---
# Installed from the vendored copy in vendor/stem (see README) instead
# of `git+https://...` so `pip install -r requirements.txt` never needs
# to reach GitHub.
-e ./vendor/stem

# --- Voice synthesis (VITS, vendored in vendor/vits) ---
numba>=0.57
librosa>=0.10
Unidecode>=1.3
jamo>=0.4
pypinyin>=0.49
jieba>=0.42
cn2an>=0.5

# Japanese text -> phoneme. Vanilla pyopenjtalk compiles from source and
# can be painful on Windows (needs a C/C++ toolchain). If `pip install`
# fails on this line, replace it with `pyopenjtalk-prebuilt` instead —
# same import name, no compiler required. See README troubleshooting.
pyopenjtalk>=0.3

# --- Image generation (Stable Diffusion) ---
# Pipa diffusers dipakai lewat server/image.py. Bobot model TIDAK diunduh
# otomatis — taruh manual di server/storage/generate/image/<id>/ (lihat README
# di folder itu). Kompatibel dengan torch 2.x yang sudah dipakai Demucs/VITS.
diffusers>=0.30
safetensors>=0.4
accelerate>=0.30
pillow>=12.0
soundfile>=0.14

# --- Background remover (rembg) ---
# Hanya dipakai via server/remover.py dengan model lokal di
# server/storage/remover/<model>/<model>.onnx — tidak pernah ke ~/.rembg
# Install di .venv saja, jangan di python system.
rembg>=2.0
onnxruntime>=1.18
pymatting>=1.0
scikit-image>=0.22
pooch>=1.8

# --- Video generation (AnimateDiff + Wan 2.1) ---
# server/video.py. AnimateDiff menempel ke model SD di storage/generate/image/;
# Wan 2.1 butuh diffusers >= 0.33 (WanPipeline), UMT5 tokenizer (sentencepiece).
# Bobot video juga TIDAK diunduh otomatis — pakai menu Setup & Runtime.
diffusers>=0.33
transformers>=4.49
sentencepiece>=0.2
ftfy>=6.2
