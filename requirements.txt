fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
# Installed from the vendored copy in backend/vendor/demucs (see below)
# instead of `git+https://...` so `pip install -r requirements.txt`
# never needs to reach GitHub.
-e ./vendor/demucs
